#!/bin/sh

set -o errexit
set -o nounset


# celery -A config.celery_app worker -l INFO

watchmedo auto-restart --directory=./apps/ --pattern=*.py --recursive -- celery -A config.celery_app worker -l INFO
